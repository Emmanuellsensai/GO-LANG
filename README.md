# GO-LANG---PRINT-SHAPE-USING-BOOL-LOOPS
Use go lang to write a function that prints a valid rectangle with a given width of x and height of y.  The function must draw the rectangles.  If x and y are positive numbers, the program should print the rectangles, otherwise, the function should print nothing.
